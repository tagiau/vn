<div id="item" class="col-sm-6 col-md-4">
	<div class="row">
	{% if (post.thumb) %}
		<a href="{{site.baseurl}}{{post.url}}">
		{% assign x = post.thumb|slice:0,4 %}
		{% if x == "http" %}
		<img src="{{post.thumb}}" alt="{{post.title}}" class="pull-left img-thumbnail"/>
		{% else %}
		<img src="{{site.asseturl}}{{post.id}}/0.jpg" class="pull-left img-thumbnail" alt="{{post.title}}"/>
		{% endif %}
		</a>
	{% endif %}
		<b>{{post.title}}</b>
		<p>{{post.excerpt|strip_html}}
		<a href="{{site.baseurl}}{{post.url}}">{{site.lang.read_more}}</a></p>
	</div>
</div>