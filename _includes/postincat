{% if (page.categories) %}<ul class="nav nav-pills nav-stacked">{% for cat in page.categories %}<li class="active"><a href="{{site.baseurl}}/{{cat}}">{{site.lang.samecat}}</a></li>{% for post in site.categories[cat] limit:include.num %}<li><a href="{{site.baseurl}}{{post.url}}">{{post.title}}</a></li>{% endfor %}{% endfor %}</ul>{% endif %}
